# 裸包名由 profile 的 node_modules 解析（`dsh plugin --profile web add <path>` 安装后生效），
- insert:
    - id: lh-data
      name: 'dsh-lh-data'
      config:
        dbPath: ''
        requireApprovalForWrites: false